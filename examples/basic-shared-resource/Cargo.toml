[package]
name = "shared-resource"
version = "0.1.0"
edition = "2021"

[dependencies]
lambda_runtime = { path = "../../lambda-runtime" }
serde = "1.0.219"
tokio = { version = "1", features = ["macros"] }
