[workspace]
resolver = "2"
members = [
    "lambda-http",
    "lambda-integration-tests",
    "lambda-runtime-api-client",
    "lambda-runtime",
    "lambda-extension",
    "lambda-events"
]

exclude = ["examples"]
