[package]
name = "extension-custom-service"
version = "0.1.0"
edition = "2021"

[dependencies]
lambda-extension = { path = "../../lambda-extension" }
serde = "1.0.217"
tokio = { version = "1", features = ["macros"] }
